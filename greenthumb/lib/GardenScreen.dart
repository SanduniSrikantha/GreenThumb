import 'package:flutter/material.dart';

class GardenScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Garden Screen'),
      ),
      body: Center(
        child: Text('Welcome to the Garden Screen'),
      ),
    );
  }
}
